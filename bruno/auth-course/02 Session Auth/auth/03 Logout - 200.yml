info:
  name: 03 Logout - 200
  type: http
  seq: 3

http:
  method: POST
  url: "{{server}}/api/v1/auth/logout"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // Sends session cookie from Bruno cookie jar (run Login first in the same session / Collection Runner).
        expect(res.status).to.equal(200);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
