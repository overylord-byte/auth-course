info:
  name: 02 Get customers - valid session - 200
  type: http
  seq: 2

http:
  method: GET
  url: "{{server}}/api/v1/customer"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // Session cookie (expected name SESSION_ID) must already be in Bruno's cookie jar for this server.
        // Run "01 Login - valid credentials - 200" first, then this request (Collection Runner or manual order).
        expect(res.status, 'Run "01 Login - valid credentials - 200" first so the SESSION_ID cookie is stored and sent.').to.equal(200);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
