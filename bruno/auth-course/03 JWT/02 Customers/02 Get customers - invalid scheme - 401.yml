info:
  name: 02 Get customers - invalid scheme - 401
  type: http
  seq: 2

http:
  method: GET
  url: "{{server}}/api/v1/customer"
  headers:
    - name: Authorization
      value: Basic test
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: expect(res.status).to.equal(401);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
